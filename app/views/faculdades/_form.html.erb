<%= form_for(@faculdade) do |f| %>
  <% if @faculdade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@faculdade.errors.count, "error") %> prohibited this faculdade from being saved:</h2>

      <ul>
      <% @faculdade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
