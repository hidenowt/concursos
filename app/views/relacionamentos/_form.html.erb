<%= form_for(@relacionamento) do |f| %>
  <% if @relacionamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@relacionamento.errors.count, "error") %> prohibited this relacionamento from being saved:</h2>

      <ul>
      <% @relacionamento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
